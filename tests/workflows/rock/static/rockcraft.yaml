name: hello
summary: Hello World (static variant)
description: A static variant of the hello ROCK for testing variant builds.
version: "1.0"
license: Apache-2.0

base: bare
build-base: ubuntu@22.04
platforms:
  amd64:

# Parts section

parts:
  hello:
    plugin: nil
    stage-packages:
      - hello_bins
